# Note: This is a pageless component. This component is for building a global navigation that each other component includes as its own nav file so they will share the same navigation overall.
name: navigation
version: latest
# nav:
# - modules/ROOT:partial$nav.adoc
